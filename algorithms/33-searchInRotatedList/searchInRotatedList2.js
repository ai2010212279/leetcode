/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 * 最差时间复杂度O(n);
 */
var search = function(nums, target) {
    var low = 0,
        high = nums.length - 1;
};

a = search([4, 6, 7, 8, 9, 10, 11, 1, 2, 3], 10);
console.log(a);
